# Test and lint tooling.
pytest>=8.0
ruff>=0.1.0

# Build small PDF fixtures for end-to-end tests at test time.
reportlab>=4.0

# spaCy English model used by the NLP service. Pinned as a direct wheel so CI
# installs it deterministically instead of relying on a runtime download.
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
